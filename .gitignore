# dependencies
node_modules/
.pnp
.pnp.*

# next.js
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts

# env — NEVER commit secrets
.env
.env.*
!.env.example

# vercel
.vercel

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# os / editor
.DS_Store
Thumbs.db
*.pem

# testing
coverage/


# large raw assets: keep originals out of git, only optimised output is committed
assets-raw/
*.blend
*.blend1

# playwright artifacts
.playwright-mcp/
design-refs/

# local snapshot of the v2 source, kept on disk only: 6.5 MB of dead code
# that would ship in every clone and every Vercel build for no benefit
backup-v2-*/

# ---- from the app's own ignore file, folded in when the app became the root
/coverage
/build
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# ---- working notes. Deliberately NOT in the repository: the plans, the brief
# and the session log are how the page was made, not what it is.
/docs/
/CLAUDE.md
/AGENTS.md
/PROGRESS.md
